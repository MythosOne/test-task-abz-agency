import { NavContainer, NavLink } from './AssingmentDeveloperSection.styled';

// import { Button } from '@/components/Button/Button';
export const AssingmentDeveloperSection = () => {
  return (
    <section>
      <h2>Assignment Developer Section</h2>
      <NavContainer>
        <NavLink href="#signUp">Sign Up</NavLink>
      </NavContainer>
      //! Сделать плавный scroll
    </section>
  );
};
